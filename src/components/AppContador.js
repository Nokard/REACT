import React from 'react'

import Contador from '../containers/Contador'

export default class AppContador extends React.Component {
    render() {
        return (
                       
                <Contador />
                          
        );
    }
}
